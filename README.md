  Developed a Flask web application for image cartoonization using pretrained Transformer models (Paprika, Hosoda,
Shinkai, and Hayao). The application loads pretrained weights to convert real photographs into cartoon-styled images
without requiring a dataset. Implemented image preprocessing, model inference, and output rendering using PyTorch,
OpenCV, and Flask for web deployment.
